---
- name: PAMM Deploy
  hosts: windows
  gather_facts: false
  vars:
    test_path: "C:\\Tests"
    
  tasks:
    - name: Update Gateway
      ansible.windows.win_copy:
        src: "{{ test_path }}\\test01\\"
        dest: "{{ test_path }}\\test02"
        remote_src: true
        force: true
