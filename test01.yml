---
- name: testing
  hosts: windows
  gather_facts: false
  vars:
    test_path: "C:\\Tests"
    
  tasks:
    - name: Create directories
      ansible.windows.win_file:
        path: "{{ test_path }}\\test01-{{ name_dir }}"
        state: directory
